import React from "react";

function CharacterCard(props) {
  return (
    <div>
      <img
        onClick={null /* You might want something here */}
        className="character-card"
        src={null /* Remove null, then character image goes here */}
      />
    </div>
  );
}

export default CharacterCard;
